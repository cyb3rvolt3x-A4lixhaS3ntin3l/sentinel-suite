# workers/

Stub only (Sprint 0 Phase A).

Future home for Go/Rust hot-path workers (DNS resolve, HTTP probe, crawl) that speak the same `sentinel_core` event schema. Python orchestrates; workers pin under `~/.sentinel/bin/`.

No workers ship in this scaffold.
