# sentinel_core

Shared library for **Sentinel Suite**: event schema v1, SQLite WAL graph per program, scope kernel MVP, engine pin under `SENTINEL_HOME`.

This is a **new clean library** — not a port of the legacy Flask `sentinel_core/` tree from ShadowsEye.

Sprint 0 Phase A scaffold. See repo root README.
