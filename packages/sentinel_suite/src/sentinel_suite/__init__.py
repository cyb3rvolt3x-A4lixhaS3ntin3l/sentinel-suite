"""Sentinel Suite meta package (brand install name ``sentinel-suite``).

Installs ``sentinel`` CLI via dependency on ``sentinel-cli``. Prefer:

    pip install -e packages/sentinel_suite
    # or future: pipx install sentinel-suite  (when published)

PyPI publish is not claimed until Founder ships wheels — see docs/INSTALL.md.
"""

__version__ = "0.1.0"
